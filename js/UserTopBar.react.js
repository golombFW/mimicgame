var React = require('react');
var Parse = require('parse').Parse;
var ParseReact = require('parse-react');

var UserTopBar = React.createClass({
    render: function () {
        return (<div className="user-topbar">topbar</div>)
    }
});

module.exports = UserTopBar;