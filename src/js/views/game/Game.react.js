var React = require('react');
var GameState = require('../../GameState.js');

var Game = React.createClass({
    render: function () {
        return (
            <div id="app-game">
                gra
            </div>
        );
    }
});

module.exports = Game;